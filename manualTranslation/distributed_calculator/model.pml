

init {
    int c_p;
    c_p = run add();


}









/*
Create a variable (c_p) in order to assign it to pid
Need a look ahead to determine the assignment is a spawn
Spawning a new process, increment out mailbox count?
Create a mapping from process name -> index

Now we've spawned a new process, we must add
it's channel to the mailbox?


*/